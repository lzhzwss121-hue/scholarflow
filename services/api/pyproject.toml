[project]
name = "scholarflow-api"
version = "0.1.0"
description = "Backend API skeleton for ScholarFlow."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0",
  "pypdf>=5.0.0"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
